// Method to work with strings.
const string = 'JavaScript is a programming language'

// Sending the two params the string to replace and the replace desire
const replaceString = string.replace('JavaScript', 'JS')
console.log(replaceString)